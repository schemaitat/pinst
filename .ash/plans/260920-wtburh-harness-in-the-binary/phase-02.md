---
id: 260920-wtburh
slug: harness-in-the-binary
phase: 2
status: Done
---

# Phase 2 — `pinst harness index`

## Goal
Generate `.ash/INDEX.md` from the binary, byte-identically to the bash
generator apart from its attribution line, and verify staleness with
`--check`.

## Why this phase exists
`index` is the first action that reads the corpus model phase 1 built and
writes something back, and it is the only one whose correctness has an exact
oracle: the current `.ash/INDEX.md`, generated by the script this plan is
replacing. Landing it before `check` means the port's first real output can be
diffed against the incumbent rather than reviewed by eye — and `check`'s
`index.stale` finding, in phase 3, needs this renderer to exist anyway. Doing
`check` first would mean implementing the renderer inside it and then pulling
it out.

## Steps
- [x] TASK-009: `src/core/harness/index.rs` (FILE-006) — `render_index(&Corpus)
      -> String`, reproducing the bash `render_index`: the `# Plan index`
      heading, the HTML attribution comment, the six-column table, one row per
      plan directory whose name matches `^[0-9]{6}-[a-z]{6}-`, in plain name
      order. Why: plan ids start `yymmdd`, so name order is already
      chronological and no sort key is needed — the same reason the bash
      version has none.
- [x] TASK-010: `src/core/harness/index.rs` — take `areas` from frontmatter as
      a list and render it `a, b, c`, matching `delist()`. Why: the bash
      version strips the brackets off the raw string; the Rust version has a
      parsed list, and must join it the same way or every row in the regenerated
      index differs.
- [x] TASK-011: `src/cli/mod.rs` and `src/cli/commands/harness.rs` — add the
      `Index` action with a `--check` flag. Without it, write
      `<root>/.ash/INDEX.md` via a temporary file and a rename; with it,
      compare and emit `index.stale` or `index.missing` without touching the
      file. Why: the write-then-rename is what `ash.sh` does, and a truncated
      index on a crashed run is worse than a stale one.
- [x] TASK-012: `src/cli/commands/harness.rs` — honour the global `--dry-run`
      on the writing path, reporting what would change and setting
      `dry_run: true` in the envelope. Why: CON-001 — every pinst command
      supports `--dry-run`, and this is the first harness action that mutates
      anything.
- [x] TASK-013: `src/core/harness/index.rs` — a test that renders the real
      corpus and asserts the output equals the committed `.ash/INDEX.md` with
      only the attribution line differing. Why: this is the oracle the phase
      exists for, and it keeps working after the cutover as a regression test
      on the renderer.

## Trade-offs & risks
The attribution line has to change — `Generated by scripts/ash.sh index` names
a file this plan deletes — so byte-identity is asserted modulo exactly that
line, and `.ash/INDEX.md` is regenerated once during phase 5 (FILE-023). Doing
it here instead would leave the tree with an index naming a command that does
not yet fully exist.

RISK-002 first bites here: `just index` becomes `cargo run -- harness index`
and now needs a build. Accepted, and recorded on the recipe.

## Done criteria
- **TEST-007**: TASK-013's test passes — the Rust renderer reproduces the
  committed index apart from the attribution line.
- **TEST-008**: `cargo run -- harness index --check` exits 0 on the current
  tree, and exits 3 with `index.stale` after a plan's `summary:` is edited.
- **TEST-009**: `cargo run -- harness index --dry-run` writes nothing (the
  file's mtime is unchanged) and reports `dry_run: true`.
- **TEST-010**: `cargo run -- harness index --json` is valid JSON under
  `python3 -m json.tool`, carrying `schema_version`, `command` and `status`.
- **TEST-011**: run from a subdirectory (`cd src && cargo run -- harness
  index --check`), the corpus is still found by the upward walk and the verdict
  is unchanged.
- **TEST-012**: `just qc` is green.
